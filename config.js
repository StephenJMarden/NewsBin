const CONFIG = {
    homepageTitle = "Top Headlines",
    searchTitle = "Search Results",
    searchError = "Sorry, your search did not return any results.",
    shortTimeout = 500,
    longTimeout = 1000
}
