const CONFIG = {
    homepageTitle = "Top Headlines",
    searchTitle = "Search Results",
    searchError = `<div style="margin: 1em auto; font-size: 0.8em;">Sorry, your search did not return any results.</div>`,
    shortTimeout = 500,
    longTimeout = 1000
}
